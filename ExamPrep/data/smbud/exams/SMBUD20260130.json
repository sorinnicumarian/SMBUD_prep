[
  {
    "id": "SMBUD20260130-e1",
    "examId": "SMBUD20260130",
    "date": "2026-01-30",
    "academicYear": "AA2526",
    "exerciseNo": 1,
    "marks": 2,
    "type": "applied-modeling",
    "topics": ["neo4j", "relational"],
    "prompt": "Consider the following ER model of a **hospital heating management system**: `Hospital(Hospital_ID PK, Name)` 1:N→`Heating System` via `Has H.Sys`; 1:N→`Building` via `Has Building`; 1:N→`Department` via `Has Dept`. `Heating System(Heater_Sys_ID PK, Heating_Power)` is specialized (total/exclusive) into `HVAC System(Hourly_KWh_Cons, Cost_Per_KWh)` and `Gas Heating System(Hourly_SQM_Cons, Cost_Per_SQM)`; it relates 1:N to `Floor` via `Heats` (attributes `Start_Time`, `End_Time`). `Building(Building_ID PK, Name, Year)` relates 1:N to `Floor` via `Has Floor`. `Floor(Floor_ID PK, Has_Elevator, Number)` relates 1:N to `Department` via `Placed Into`. `Department(Department_ID PK, Specialty, N_Beds, Head_Physician_ID)` relates N:1 to `Person(P_ID PK, Email)` via `Works For` (attribute `Role`).\n\nConsider the entities **Heating System** (and its child entities), **Floor**, **Building**, and **Department**. Provide a written textual representation (no graphical sketches) of the graph model, using the typical Neo4j notation for nodes, edges `-[ ]->`, and attributes `{ }`. Include data types for attributes.",
    "solution": "```\n// Nodes\n(:HeatingSystem {heater_sys_id: string, heating_power: int})\n(:HVACSystem {heater_sys_id: string, heating_power: int, hourly_kwh_cons: float, cost_per_kwh: float})\n(:GasHeatingSystem {heater_sys_id: string, heating_power: int, hourly_sqm_cons: float, cost_per_sqm: float})\n(:Floor {floor_id: string, has_elevator: boolean, number: int})\n(:Building {building_id: string, name: string, year: int})\n(:Department {department_id: string, specialty: string, n_beds: int, head_physician_id: string})\n\n// HVACSystem and GasHeatingSystem carry the HeatingSystem label too (multi-label\n// node = ISA specialization), e.g. (h:HeatingSystem:HVACSystem {...}) — shown above\n// as separate node types only to make each one's own attributes explicit.\n\n// Edges\n(:HeatingSystem)-[:HEATS {start_time: datetime, end_time: datetime}]->(:Floor)\n(:Building)-[:HAS_FLOOR]->(:Floor)\n(:Floor)-[:PLACED_INTO]->(:Department)\n```\n\nNote the ISA hierarchy: in Neo4j there's no native inheritance, so `HVACSystem`/`GasHeatingSystem` nodes carry **both** the specific label and the generic `HeatingSystem` label (multi-labeling), each with the parent's attributes plus its own.",
    "solutionSource": "authored"
  },
  {
    "id": "SMBUD20260130-e2",
    "examId": "SMBUD20260130",
    "date": "2026-01-30",
    "academicYear": "AA2526",
    "exerciseNo": 2,
    "marks": 2,
    "type": "applied-modeling",
    "topics": ["neo4j"],
    "prompt": "Using the graph model from the previous exercise (Heating System/HVAC/Gas, Floor, Building, Department), write a Cypher query that returns, for every Department occupying more than 2 Floors in the same Building, the total heating power of all their Heaters. Filter the results to show only those Departments whose total power exceeds 0.5 MWh and sort them in descending order by power.",
    "solution": "```cypher\nMATCH (b:Building)-[:HAS_FLOOR]->(f:Floor)-[:PLACED_INTO]->(d:Department)\nWITH b, d, count(DISTINCT f) AS floorsInBuilding, collect(f) AS floors\nWHERE floorsInBuilding > 2\nUNWIND floors AS f2\nMATCH (h:HeatingSystem)-[:HEATS]->(f2)\nWITH d, sum(h.heating_power) AS total_heating_power_kw\nWHERE total_heating_power_kw > 500  // 0.5 MWh expressed in kW, assuming heating_power is stored in kW\nRETURN d.department_id, d.specialty, total_heating_power_kw\nORDER BY total_heating_power_kw DESC\n```\n\nKey points: group per `(Building, Department)` pair first to count floors *within the same building* (not globally), then only sum heating power for departments that pass the `>2 floors` filter, before applying the `>0.5 MWh` threshold and sorting.",
    "solutionSource": "authored"
  },
  {
    "id": "SMBUD20260130-e3",
    "examId": "SMBUD20260130",
    "date": "2026-01-30",
    "academicYear": "AA2526",
    "exerciseNo": 3,
    "marks": 2,
    "type": "applied-modeling",
    "topics": ["neo4j"],
    "prompt": "Using the same hospital heating graph model, write a Cypher query that returns all Buildings built after 1967 with a total number of beds (summed across their Departments) greater than or equal to 20, located on floors with an elevator **or** located on the ground level (floor number 0).",
    "solution": "```cypher\nMATCH (b:Building)-[:HAS_FLOOR]->(f:Floor)-[:PLACED_INTO]->(d:Department)\nWHERE b.year > 1967 AND (f.has_elevator = true OR f.number = 0)\nWITH b, sum(d.n_beds) AS total_beds\nWHERE total_beds >= 20\nRETURN b.building_id, b.name, b.year, total_beds\n```\n\nThe accessibility filter (`has_elevator` OR ground floor) applies to the *floor* the department sits on, and must be evaluated before aggregating beds per building, otherwise beds from inaccessible floors would be included.",
    "solutionSource": "authored"
  },
  {
    "id": "SMBUD20260130-e4",
    "examId": "SMBUD20260130",
    "date": "2026-01-30",
    "academicYear": "AA2526",
    "exerciseNo": 4,
    "marks": 2,
    "type": "applied-modeling",
    "topics": ["columnar"],
    "prompt": "Consider the entities Heating System (and its child entities), Floor, and Department from the hospital heating ER model (Exercise 1). Define the database structure so that one can query Heaters simultaneously by Floor number and Department specialty, sorting the results by total heating power. Write the specification of the tables of a Cassandra deployment that stores the required data. Do not use indexes.",
    "solution": "```sql\nCREATE TABLE heaters_by_floor_and_department (\n  floor_number int,\n  department_specialty text,\n  heater_sys_id uuid,\n  total_heating_power int,\n  heater_type text,\n  -- any other heater attributes...\n  PRIMARY KEY (\n    (floor_number, department_specialty), -- partition key: query by floor + department\n    total_heating_power,                  -- first clustering key: enables sorting\n    heater_sys_id                          -- second one: ensures uniqueness (two heaters\n                                            -- in the same floor+department with the same power)\n  )\n) WITH CLUSTERING ORDER BY (total_heating_power DESC); -- sort heaters by power\n```\n\n**Grading notes (penalized if):** not including both `floor_number` and `department_specialty` in the partition key · incorrect partition-key composition (e.g. adding `heater_sys_id` to it) · using a secondary index instead of modeling the partition key correctly (explicitly forbidden by the question) · incorrect `PRIMARY KEY` syntax · missing `total_heating_power` as the first clustering key · missing `WITH CLUSTERING ORDER BY` · designing a normalized/relational schema instead of a query-driven Cassandra table · using `ALLOW FILTERING` instead of modeling correctly · adding unnecessary tables without justification.",
    "solutionSource": "exam"
  },
  {
    "id": "SMBUD20260130-e5",
    "examId": "SMBUD20260130",
    "date": "2026-01-30",
    "academicYear": "AA2526",
    "exerciseNo": 5,
    "marks": 1,
    "type": "applied-modeling",
    "topics": ["columnar"],
    "prompt": "Using the `heaters_by_floor_and_department` Cassandra table from the previous exercise, write a CQL query to collect all the Gas Heating Systems running for \"cardiology\" Departments. If any further operation is required, write its CQL code.",
    "solution": "The existing table's partition key is `(floor_number, department_specialty)`, so it can't be queried by `department_specialty` alone without `ALLOW FILTERING` (undesirable) or scanning all floors. A **query-driven** fix is to add a second table keyed only by department specialty (and heater type), denormalizing the same rows:\n\n```sql\nCREATE TABLE heaters_by_department_and_type (\n  department_specialty text,\n  heater_type text,\n  heater_sys_id uuid,\n  floor_number int,\n  total_heating_power int,\n  PRIMARY KEY ((department_specialty, heater_type), heater_sys_id)\n);\n\nSELECT * FROM heaters_by_department_and_type\nWHERE department_specialty = 'cardiology' AND heater_type = 'gas';\n```\n\n(Writes to the base heater data must now also insert into this second table — standard Cassandra denormalize-for-every-query-pattern practice.)",
    "solutionSource": "authored"
  },
  {
    "id": "SMBUD20260130-e6",
    "examId": "SMBUD20260130",
    "date": "2026-01-30",
    "academicYear": "AA2526",
    "exerciseNo": 6,
    "marks": 2,
    "type": "applied-modeling",
    "topics": ["mongodb"],
    "prompt": "Consider the entities Hospital, Building, Department, and Person from the hospital heating ER model (Exercise 1) (a Hospital has many Buildings and many Departments; a Department has many Persons working for it via `Works For`). How would you implement the nesting of the entities and the relationships between them in MongoDB? Provide a simple documental representation. Define ALL attributes and their types, and provide additional comments when needed. You must design the relationships without using the MongoDB `$lookup` operation.",
    "solution": "```json\n// hospitals collection — buildings and departments embedded (no $lookup needed to read them)\n{\n  \"hospital_id\": \"uuid\",\n  \"name\": \"string\",\n  \"buildings\": [\n    { \"building_id\": \"uuid\", \"name\": \"string\", \"year\": \"int\" }\n  ],\n  \"departments\": [\n    {\n      \"department_id\": \"uuid\",\n      \"specialty\": \"string\",\n      \"n_beds\": \"int\",\n      \"head_physician_id\": \"uuid\",\n      \"persons\": [\n        // embedded — a department can have many persons working for it.\n        // Note: if a person works for multiple departments, their data is\n        // duplicated across departments (denormalized on purpose, no $lookup).\n        { \"p_id\": \"uuid\", \"email\": \"string\", \"role\": \"string\" }\n      ]\n    }\n  ]\n}\n```\n\nBuildings and Departments are embedded arrays inside their parent Hospital (1:N, bounded, always read together with the hospital); Persons are embedded inside each Department they work for, with the `role` attribute (from `Works For`) stored directly on the embedded person subdocument. This achieves 1-hop reads for all four entities without any `$lookup`, at the cost of duplicating a person's data if they work in more than one department.",
    "solutionSource": "authored"
  },
  {
    "id": "SMBUD20260130-e7",
    "examId": "SMBUD20260130",
    "date": "2026-01-30",
    "academicYear": "AA2526",
    "exerciseNo": 7,
    "marks": 2,
    "type": "applied-modeling",
    "topics": ["mongodb"],
    "prompt": "Using the hospitals/departments/persons document model from the previous exercise, write a MongoDB aggregation pipeline to collect, for each hospital with more than 10 buildings, the number of departments with more than 25 people. Use only the pipeline stages discussed during the practical lectures ($match, $project, $group, $unwind, $addFields, $count, etc.).",
    "solution": "```js\ndb.hospitals.aggregate([\n  { $addFields: { building_count: { $size: \"$buildings\" } } },\n  { $match: { building_count: { $gt: 10 } } },\n  { $addFields: {\n      big_departments: {\n        $filter: {\n          input: \"$departments\",\n          as: \"d\",\n          cond: { $gt: [{ $size: \"$$d.persons\" }, 25] }\n        }\n      }\n  } },\n  { $project: {\n      _id: 0,\n      hospital_id: 1,\n      name: 1,\n      departments_over_25: { $size: \"$big_departments\" }\n  } }\n])\n```\n\nThe pipeline first computes each hospital's building count and filters (`$match`) to hospitals with more than 10, then uses `$filter` to keep only departments whose embedded `persons` array has more than 25 elements, and finally projects the count of such departments per hospital.",
    "solutionSource": "authored"
  },
  {
    "id": "SMBUD20260130-e8",
    "examId": "SMBUD20260130",
    "date": "2026-01-30",
    "academicYear": "AA2526",
    "exerciseNo": 8,
    "marks": 2,
    "type": "applied-modeling",
    "topics": ["mongodb"],
    "prompt": "Using the same document model, write a query to return the total number of departments each person with role 'maintenance' works for. Return only the top 10 people working in the highest number of departments. Use only the pipeline stages discussed during the practical lectures.",
    "solution": "```js\ndb.hospitals.aggregate([\n  { $unwind: \"$departments\" },\n  { $unwind: \"$departments.persons\" },\n  { $match: { \"departments.persons.role\": \"maintenance\" } },\n  { $group: {\n      _id: \"$departments.persons.p_id\",\n      department_count: { $sum: 1 }\n  } },\n  { $sort: { department_count: -1 } },\n  { $limit: 10 }\n])\n```\n\n`$unwind` twice to flatten hospitals → departments → persons into one document per (department, person) pair, `$match` on role, then `$group` by person id counting how many department-documents they appear in (equivalent to \"how many departments they work for\", assuming the same person's `p_id` is duplicated once per department per the embedding design from Exercise 6).",
    "solutionSource": "authored"
  },
  {
    "id": "SMBUD20260130-e9",
    "examId": "SMBUD20260130",
    "date": "2026-01-30",
    "academicYear": "AA2526",
    "exerciseNo": 9,
    "marks": 1,
    "type": "applied-modeling",
    "topics": ["spark"],
    "prompt": "Consider a tabular representation of Hospital data with columns `Hospital_ID`, `Name`, `Heating_Systems` (an array of heater IDs), e.g. row `(\"HOSP_1\", ..., [\"HS_1\", \"HS_2\", ...])`. Write Python (PySpark) code to create a DataFrame for this table and to add one row to it.",
    "solution": "```python\nfrom pyspark.sql import SparkSession\nfrom pyspark.sql.types import StructType, StructField, StringType, ArrayType\n\nspark = SparkSession.builder.getOrCreate()\n\nschema = StructType([\n    StructField(\"Hospital_ID\", StringType(), True),\n    StructField(\"Name\", StringType(), True),\n    StructField(\"Heating_Systems\", ArrayType(StringType()), True)\n])\n\ndata = [\n    (\"HOSP_1\", \"Hospital A\", [\"HS_1\", \"HS_2\"]),\n    (\"HOSP_2\", \"Hospital B\", [\"HS_3\"])\n]\n\ndf = spark.createDataFrame(data, schema)\n\n# add one row\nnew_row = [(\"HOSP_3\", \"Hospital C\", [\"HS_4\", \"HS_5\"])]\ndf_new = df.union(spark.createDataFrame(new_row, schema))\n```\n\n**Acceptable with penalty** (pandas instead of PySpark):\n```python\nimport pandas as pd\ndata = {\"Hospital_ID\": [\"HOSP_1\", \"HOSP_2\"], \"Name\": [\"Hospital A\", \"Hospital B\"],\n        \"Heating_Systems\": [[\"HS_1\", \"HS_2\"], [\"HS_3\"]]}\ndf = pd.DataFrame(data)\nnew_row = {\"Hospital_ID\": \"HOSP_3\", \"Name\": \"Hospital C\", \"Heating_Systems\": [\"HS_4\", \"HS_5\"]}\ndf = pd.concat([df, pd.DataFrame([new_row])], ignore_index=True)\n```\n\n**Grading notes (penalized):** incorrect DataFrame/schema definition · DataFrame not referenced properly in the code · use of row-insertion methods that don't exist in Spark (`insert`, `add`) · use of SQL-like foreign-key syntax · evident syntax errors.",
    "solutionSource": "exam"
  },
  {
    "id": "SMBUD20260130-e10",
    "examId": "SMBUD20260130",
    "date": "2026-01-30",
    "academicYear": "AA2526",
    "exerciseNo": 10,
    "marks": 2,
    "type": "applied-modeling",
    "topics": ["spark"],
    "prompt": "Given a `hospitals` DataFrame (`Hospital_ID`, `Name`, `Heating_Systems` array) and a `heating` DataFrame (`Heat_ID`, `H_KW_Cons`, `Heat_Power`), write a PySpark query (no SQL syntax) to compute and return the total number of **unique** `H_KW_Cons` values for each Hospital whose name includes the word \"Care\".",
    "solution": "```python\nfrom pyspark.sql import functions as F\n\nresult = (\n    hospitalsDF\n    .filter(F.col(\"Name\").contains(\"Care\"))\n    .withColumn(\"Heat_ID\", F.explode(\"Heating_Systems\"))\n    .join(heatingDF, on=\"Heat_ID\", how=\"inner\")\n    .groupBy(\"Hospital_ID\", \"Name\")\n    .agg(F.countDistinct(\"H_KW_Cons\").alias(\"unique_H_KWh_Consumption\"))\n)\n```\n\n**Grading notes (penalized):** use of SQL-like syntax · evident syntax errors · use of pseudocode · missing or incorrect `groupBy` · unnecessary use of `sum` · missing `.explode()` on the heating-systems array · use of RDDs instead of DataFrames · missing aggregation.",
    "solutionSource": "exam"
  },
  {
    "id": "SMBUD20260130-e11",
    "examId": "SMBUD20260130",
    "date": "2026-01-30",
    "academicYear": "AA2526",
    "exerciseNo": 11,
    "marks": 2,
    "type": "applied-modeling",
    "topics": ["spark"],
    "prompt": "Using the same `hospitals`/`heating` DataFrames, write a PySpark query (no SQL syntax) to compute and return the total `Heat_Power` for each hospital, returning only the **name** of the hospitals whose total exceeds 100.",
    "solution": "```python\nfrom pyspark.sql import functions as F\n\nfinal_df = (\n    hospital_df\n    .withColumn(\"Heat_ID\", F.explode(\"Heating_Systems\"))\n    .join(heating_df, \"Heat_ID\")\n    .groupBy(\"Hospital_ID\", \"Name\")\n    .agg(F.sum(\"Heat_Power\").alias(\"Total_Heat_Power\"))\n    .filter(F.col(\"Total_Heat_Power\") > 100)\n    .select(\"Name\")\n)\n```\n\n**Grading notes (penalized):** SQL-like syntax · syntax errors · pseudocode · missing/incorrect `groupBy` · missing `sum` · missing `.explode()` · RDDs instead of DataFrames · missing aggregation · missing join · missing filter on `Total_Heat_Power > 100` · missing final `select(\"Name\")`.",
    "solutionSource": "exam"
  },
  {
    "id": "SMBUD20260130-e12",
    "examId": "SMBUD20260130",
    "date": "2026-01-30",
    "academicYear": "AA2526",
    "exerciseNo": 12,
    "marks": 1,
    "type": "applied-modeling",
    "topics": ["keyvalue"],
    "prompt": "Using Redis, create a single key-value pair to store all the data of one Gas Heater, with a TTL of 100 seconds, including all the attributes describing the object.",
    "solution": "```\nHSET gas_heater_01 brand \"ThermoMax\" model \"TX-500\" hourly_sqm_cons 5 cost_per_sqm 10 heating_power 100\nEXPIRE gas_heater_01 100\n```\n\n**Partial credit:** using `HSET` with a missing/incorrect TTL (e.g. trying `EX` with `HSET`, which isn't valid); or `SET` with a clearly JSON-encoded object containing all attributes plus `EX 100`, e.g. `SET gas_heater_01 '{\"id\":\"GH-001\",\"brand\":\"ThermoMax\", ...}' EX 100`; or deprecated commands like `HMSET` with no TTL.\n\n**Zero points:** unrelated Redis commands (`SADD`, `LPUSH`, …) · splitting attributes across multiple keys · `SET` with no clear JSON encoding · conceptual text with no actual Redis command.",
    "solutionSource": "exam"
  },
  {
    "id": "SMBUD20260130-e13",
    "examId": "SMBUD20260130",
    "date": "2026-01-30",
    "academicYear": "AA2526",
    "exerciseNo": 13,
    "marks": 2,
    "type": "applied-modeling",
    "topics": ["keyvalue"],
    "prompt": "Using Redis, create a structure to store all Department specialties sorted by their foundation year, then query it to get the oldest one.",
    "solution": "```\nZADD dept_specialties 1975 \"Cardiology\" 1980 \"Neurology\" 1990 \"Oncology\"\nZRANGE dept_specialties 0 0\n```\nor equivalently, if insertion order already reflects chronological order:\n```\nLPUSH dept_specialties \"Cardiology\" \"Neurology\" \"Oncology\"\nLRANGE dept_specialties -1 -1\n```\n\nA **sorted set** (`ZADD`, scored by year) is the cleanest fit since it natively keeps entries ordered by a numeric score and `ZRANGE 0 0` directly retrieves the minimum (oldest); a plain `LIST` also works but only if insertion order is guaranteed to already be sorted.\n\n**Partial credit:** `LIST` used without preserving foundation-year order (but still queried with `LRANGE`) · `ZADD` with incorrect/missing scores or a wrong-but-intentional `ZRANGE` · using `POP` operations (`LPOP`/`RPOP`/`ZPOPMIN`) to retrieve the oldest.\n\n**Zero points:** unrelated data types (`SADD`, `HSET`, …) · specialties split across multiple keys · conceptual text with no Redis commands · a solution that neither maintains order nor retrieves the oldest.",
    "solutionSource": "exam"
  },
  {
    "id": "SMBUD20260130-e14",
    "examId": "SMBUD20260130",
    "date": "2026-01-30",
    "academicYear": "AA2526",
    "exerciseNo": 14,
    "marks": 2,
    "type": "applied-modeling",
    "topics": ["keyvalue"],
    "prompt": "Using Redis, create a set to store all Department specialties for Hospital 1, and another set for Hospital 2. Then query for the specialties present in **both** hospitals.",
    "solution": "```\nSADD dept_hosp1_specialties \"cardiology\" \"neurology\" \"oncology\"\nSADD dept_hosp2_specialties \"neurology\" \"oncology\" \"pediatrics\"\n\nSINTER dept_hosp1_specialties dept_hosp2_specialties\n```\n\n`SADD` builds an unordered set of specialties per hospital, and `SINTER` (or `SINTERSTORE` into a new key, if the intersection needs to be persisted) directly computes set intersection — the native Redis operation for \"present in both\", rather than manually filtering, since sets do not need to be sorted and duplicates are automatically deduplicated by the data structure itself.",
    "solutionSource": "authored"
  }
]
