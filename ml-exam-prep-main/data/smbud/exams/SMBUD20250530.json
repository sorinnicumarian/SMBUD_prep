[
  {
    "id": "SMBUD20250530-e1",
    "examId": "SMBUD20250530",
    "date": "2025-05-30",
    "academicYear": "AA2425",
    "exerciseNo": 1,
    "marks": 1,
    "type": "applied-modeling",
    "topics": ["columnar"],
    "prompt": "Consider the ER model tracking puzzles' production and sales (Company `Produce`→Puzzle `1:N`, Puzzle `Buy`↔User `N:M`, Image `Based`→Puzzle `1:N` with Painting as a specialization of Image and Creator as a specialization of User, Puzzle `Fit`↔Frame `N:M`).\n\nSuppose you expect to record a large number of purchases of puzzles and you decide to store them in a columnar database. Assume you want to run typical queries by searching by the puzzle IDs and by User City or Country. Write the specification of the tables (and other artifacts if necessary) of a Cassandra deployment that stores the required data.",
    "solution": "Cassandra is **query-first**: since the workload needs to look up purchases by `puzzle_id`, by `user_city`, and by `user_country`, and equality lookups only hit the *partition key*, we denormalize the `Buy` relationship into **three purpose-built tables**, one per access pattern:\n\n```sql\nCREATE TABLE purchases_by_puzzle (\n    puzzle_id     TEXT,\n    buy_date      TIMESTAMP,\n    user_id       TEXT,\n    user_city     TEXT,\n    user_country  TEXT,\n    PRIMARY KEY (puzzle_id, buy_date, user_id)\n) WITH CLUSTERING ORDER BY (buy_date DESC);\n\nCREATE TABLE purchases_by_city (\n    user_city     TEXT,\n    buy_date      TIMESTAMP,\n    user_id       TEXT,\n    puzzle_id     TEXT,\n    PRIMARY KEY (user_city, buy_date, user_id)\n) WITH CLUSTERING ORDER BY (buy_date DESC);\n\nCREATE TABLE purchases_by_country (\n    user_country  TEXT,\n    buy_date      TIMESTAMP,\n    user_id       TEXT,\n    puzzle_id     TEXT,\n    PRIMARY KEY (user_country, buy_date, user_id)\n) WITH CLUSTERING ORDER BY (buy_date DESC);\n```\n\nEach table is partitioned on the attribute the queries filter by (`puzzle_id`, `user_city`, `user_country`), with `buy_date`/`user_id` as clustering columns to keep purchases ordered and uniquely identified within a partition. The three tables are redundant copies of the same `Buy` facts (write amplification is the accepted trade-off in Cassandra for read efficiency); an application-side or Cassandra-triggers/CDC mechanism keeps them in sync on every new purchase.",
    "solutionSource": "authored"
  },
  {
    "id": "SMBUD20250530-e2",
    "examId": "SMBUD20250530",
    "date": "2025-05-30",
    "academicYear": "AA2425",
    "exerciseNo": 2,
    "marks": 1,
    "type": "applied-modeling",
    "topics": ["neo4j"],
    "prompt": "Consider the entities User, Creator, Image, Painting, and Puzzle from the ER model (Exercise 1) (Creator is a specialization of User with `nickname`, `creatorSince`; Painting is a specialization of Image with `artistName`, `artistSurname`, `paintingLocation`; Image `Provide`→Creator `0:N`/`1:N` with attribute `originalWork`; Image `Based`→Puzzle `1:N`/`1:1`).\n\nSuppose you want to store the respective data instances in a graph database. Sketch a graph model/example describing the nodes, main attributes, and edges. Either show an example graph or a graph with types.",
    "solution": "Property-graph design (Neo4j notation: `(:Label {props})` for nodes, multi-labels for the ISA hierarchies, `-[:REL {props}]->` for relationships):\n\n```\n(:User {personalId, name, surname, age, birthDate, city, country})\n(:User:Creator {personalId, ..., nickname, creatorSince})\n(:Image {title, width, height, description})\n(:Image:Painting {title, ..., artistName, artistSurname, paintingLocation})\n(:Puzzle {puzzleId, name, collectionName, numberOfPieces, width, height, description})\n\n(:Image)-[:PROVIDE {originalWork}]->(:User:Creator)\n(:Image)-[:BASED_ON]->(:Puzzle)\n```\n\nA `User` who is also a creator gets the extra `:Creator` label (multi-label ISA) instead of a separate node, so their `PROVIDE` edges to the images they uploaded stay on the same node used for `BUY` edges elsewhere. Similarly a `Painting` is an `Image` with the extra `:Painting` label carrying the artist/location attributes. `PROVIDE` mirrors the ER `Image (1,N) — Creator (0,N)`: an image is uploaded by 0..N creators (as given) while being usable multiple times; `BASED_ON` mirrors `Image (1,N) — Puzzle (1,1)`: each puzzle is based on exactly one image, and one image can be the basis of several puzzles.",
    "solutionSource": "authored"
  },
  {
    "id": "SMBUD20250530-e3",
    "examId": "SMBUD20250530",
    "date": "2025-05-30",
    "academicYear": "AA2425",
    "exerciseNo": 2,
    "marks": 2,
    "type": "applied-modeling",
    "topics": ["neo4j"],
    "prompt": "Using the graph model from the previous point, write a Cypher query to compute the total number of paintings' images uploaded by each creator whose location is \"Van Gogh Museum\". Consider only the creators who have been creators since a date before 02/01/2024.",
    "solution": "```cypher\nMATCH (i:Image:Painting)-[:PROVIDE]->(c:User:Creator)\nWHERE i.paintingLocation = \"Van Gogh Museum\"\n  AND c.creatorSince < date(\"2024-01-02\")\nRETURN c.personalId AS creator, c.nickname AS nickname, count(i) AS totalPaintingImages\nORDER BY totalPaintingImages DESC\n```\n\"Location\" here refers to the `paintingLocation` attribute carried by the `:Painting`-labeled images (only paintings have a location, per the ER model), so the filter is applied on `i.paintingLocation`. The `creatorSince` filter is applied on the creator node, and images are counted per creator via `count(i)` after the match.",
    "solutionSource": "authored"
  },
  {
    "id": "SMBUD20250530-e4",
    "examId": "SMBUD20250530",
    "date": "2025-05-30",
    "academicYear": "AA2425",
    "exerciseNo": 2,
    "marks": 2,
    "type": "applied-modeling",
    "topics": ["neo4j"],
    "prompt": "Write a Cypher query to compute the total number of puzzles bought by each user who created it (i.e., a puzzle is counted towards a user if they both uploaded the image and bought the resulting puzzle), considering only users who bought at least 50 puzzles, whose number of pieces is greater than 1'000.",
    "solution": "```cypher\nMATCH (u:User:Creator)<-[:PROVIDE]-(i:Image)-[:BASED_ON]->(p:Puzzle),\n      (u)-[:BUY]->(p)\nWHERE p.numberOfPieces > 1000\nWITH u, count(DISTINCT p) AS totalCreatedAndBought\nWHERE totalCreatedAndBought >= 50\nRETURN u.personalId AS user, u.nickname AS nickname, totalCreatedAndBought\nORDER BY totalCreatedAndBought DESC\n```\nA puzzle counts towards a user only if the *same* `u` node is reached both via `PROVIDE` (they uploaded the base image) and via `BUY` (they bought the resulting puzzle) — matching both patterns on the shared variables `u` and `p` enforces this. The `numberOfPieces > 1000` filter restricts which puzzles are eligible before counting, and the final `WHERE` on the aggregated count keeps only users reaching the 50-puzzle threshold.",
    "solutionSource": "authored"
  },
  {
    "id": "SMBUD20250530-e5",
    "examId": "SMBUD20250530",
    "date": "2025-05-30",
    "academicYear": "AA2425",
    "exerciseNo": 3,
    "marks": 1,
    "type": "applied-modeling",
    "topics": ["elasticsearch"],
    "prompt": "Consider the Puzzle entity (Puzzle ID, Name, Collection Name, Number of Pieces, Width, Height, Description). Provide the complete mapping of the index for the considered entity (i.e., field name, field type, the structure of the mapping, etc.).",
    "solution": "```json\nPUT /puzzles\n{\n  \"mappings\": {\n    \"properties\": {\n      \"puzzleId\":        { \"type\": \"keyword\" },\n      \"name\":            { \"type\": \"text\" },\n      \"collectionName\":  { \"type\": \"keyword\" },\n      \"numberOfPieces\":  { \"type\": \"integer\" },\n      \"width\":           { \"type\": \"float\" },\n      \"height\":          { \"type\": \"float\" },\n      \"description\":     { \"type\": \"text\", \"analyzer\": \"standard\" }\n    }\n  }\n}\n```\n`puzzleId` and `collectionName` are `keyword` (exact-match / aggregatable, not tokenized — needed later to bucket by collection), `numberOfPieces`/`width`/`height` are numeric types, and `name`/`description` are free-text `text` fields so they are analyzed and full-text searchable.",
    "solutionSource": "authored"
  },
  {
    "id": "SMBUD20250530-e6",
    "examId": "SMBUD20250530",
    "date": "2025-05-30",
    "academicYear": "AA2425",
    "exerciseNo": 3,
    "marks": 2,
    "type": "applied-modeling",
    "topics": ["elasticsearch"],
    "prompt": "Write a query to compute the total number of puzzles for each collection name.",
    "solution": "```json\nGET /puzzles/_search\n{\n  \"size\": 0,\n  \"aggs\": {\n    \"by_collection\": {\n      \"terms\": {\n        \"field\": \"collectionName\",\n        \"size\": 100\n      }\n    }\n  }\n}\n```\n`size: 0` suppresses the hit list since only the aggregation result is needed. The `terms` aggregation buckets documents by the (non-analyzed) `collectionName` keyword field and returns, for each bucket, `doc_count` — the number of puzzles in that collection.",
    "solutionSource": "authored"
  },
  {
    "id": "SMBUD20250530-e7",
    "examId": "SMBUD20250530",
    "date": "2025-05-30",
    "academicYear": "AA2425",
    "exerciseNo": 4,
    "marks": 1.5,
    "type": "applied-modeling",
    "topics": ["spark"],
    "prompt": "Consider the following `Frame` and `Puzzle` tables:\n\n- `Frame(FrameID [PK], Width, Height, Material, Color)`\n- `Puzzle(FrameID [FK, array e.g. [X, Y, …]], PuzzleID [PK], Name, CollectionName, NumberOfPieces, Width, Height, Description)`\n\nEach puzzle row stores, in `FrameID`, the array of frame IDs that fit it (the `Fit` N:M relationship denormalized into `Puzzle`).\n\nWrite a SparkQL (Spark SQL) query to return the frames that perfectly fit the puzzle they are fit for.",
    "solution": "```sql\nSELECT f.*\nFROM puzzle p\nLATERAL VIEW explode(p.FrameID) exploded AS frameId\nJOIN frame f ON f.FrameID = exploded.frameId\nWHERE f.Width = p.Width AND f.Height = p.Height\n```\n`explode` unnests each puzzle's `FrameID` array into one row per candidate frame, the join brings in that frame's own dimensions, and \"perfectly fit\" is expressed as an exact match between the frame's `Width`/`Height` and the puzzle's `Width`/`Height`.",
    "solutionSource": "authored"
  },
  {
    "id": "SMBUD20250530-e8",
    "examId": "SMBUD20250530",
    "date": "2025-05-30",
    "academicYear": "AA2425",
    "exerciseNo": 4,
    "marks": 1.5,
    "type": "applied-modeling",
    "topics": ["spark"],
    "prompt": "Write a SparkQL (Spark SQL) query to count the total number of (distinct) frames with a width and height greater than 100 and 70 cm, respectively, that are associated with a puzzle from the \"Landscape Collection\".",
    "solution": "```sql\nSELECT COUNT(DISTINCT f.FrameID) AS totalFrames\nFROM puzzle p\nLATERAL VIEW explode(p.FrameID) exploded AS frameId\nJOIN frame f ON f.FrameID = exploded.frameId\nWHERE p.CollectionName = 'Landscape Collection'\n  AND f.Width > 100\n  AND f.Height > 70\n```\nAs before, `explode` turns each puzzle's array of associated `FrameID`s into individual rows so they can be joined against `frame`; the `WHERE` clause restricts to puzzles in the \"Landscape Collection\" and frames wider than 100 cm and taller than 70 cm, and `COUNT(DISTINCT f.FrameID)` avoids double-counting a frame associated with more than one qualifying puzzle.",
    "solutionSource": "authored"
  },
  {
    "id": "SMBUD20250530-e9",
    "examId": "SMBUD20250530",
    "date": "2025-05-30",
    "academicYear": "AA2425",
    "exerciseNo": 5,
    "marks": 1,
    "type": "applied-modeling",
    "topics": ["mongodb"],
    "prompt": "Consider the entities Puzzle, Image, Frame, and Company. How many collections would you define within a documental database (clearly state it)? How would you implement the relations between the concepts? Provide a simple documental representation.",
    "solution": "**Number of collections: 4** — `companies`, `images`, `frames`, `puzzles` — one per entity, because all the relations among them are either `1:N` with potentially many/unbounded children on the \"many\" side (`Company Produce Puzzle`, `Image Based Puzzle`) or `N:M` (`Puzzle Fit Frame`), so embedding one inside another would either duplicate data across many parents or grow one document unboundedly. Relations are therefore implemented by **reference** (storing the related `_id`(s), MongoDB's analogue of a foreign key):\n\n```json\n// companies\n{ \"_id\": \"C1\", \"name\": \"PuzzleCo\", \"address\": \"...\", \"phoneNumber\": \"...\", \"biography\": \"...\" }\n\n// images\n{ \"_id\": \"I1\", \"title\": \"Starry Night\", \"width\": 30, \"height\": 40, \"description\": \"...\",\n  \"artistName\": \"Vincent\", \"artistSurname\": \"van Gogh\", \"paintingLocation\": \"Van Gogh Museum\" }\n\n// frames\n{ \"_id\": \"F1\", \"width\": 50, \"height\": 70, \"material\": \"wood\", \"color\": \"black\" }\n\n// puzzles\n{ \"_id\": \"P1\", \"name\": \"Starry Night 1000pc\", \"collectionName\": \"Museum Collection\",\n  \"numberOfPieces\": 1000, \"width\": 50, \"height\": 70, \"description\": \"...\",\n  \"companyId\": \"C1\",\n  \"imageId\": \"I1\",\n  \"frameIds\": [\"F1\", \"F2\"],\n  \"productionDate\": \"1998-06-01\" }\n```\n`puzzles` holds a single `companyId` reference (its producer, `1:1` on the puzzle side of `Produce`), a single `imageId` reference (the image it is `Based` on, also `1:1` on the puzzle side), and a `frameIds` array (the `N:M` `Fit` relationship) referencing the frames that fit it.",
    "solutionSource": "authored"
  },
  {
    "id": "SMBUD20250530-e10",
    "examId": "SMBUD20250530",
    "date": "2025-05-30",
    "academicYear": "AA2425",
    "exerciseNo": 5,
    "marks": 1.5,
    "type": "applied-modeling",
    "topics": ["mongodb"],
    "prompt": "Write a query to compute the total number of puzzles produced by each company, considering only puzzles produced after 30/04/1995 and with more than 500 pieces, and returning only the companies that produced more than 100 puzzles.",
    "solution": "```javascript\ndb.puzzles.aggregate([\n  { $match: {\n      productionDate: { $gt: ISODate(\"1995-04-30\") },\n      numberOfPieces: { $gt: 500 }\n  }},\n  { $group: { _id: \"$companyId\", totalPuzzles: { $sum: 1 } } },\n  { $match: { totalPuzzles: { $gt: 100 } } }\n])\n```\nThe first `$match` filters puzzles produced after 1995-04-30 with more than 500 pieces, `$group` counts them per `companyId`, and the second `$match` (the aggregation-pipeline equivalent of `HAVING`) keeps only companies whose count exceeds 100.",
    "solutionSource": "authored"
  },
  {
    "id": "SMBUD20250530-e11",
    "examId": "SMBUD20250530",
    "date": "2025-05-30",
    "academicYear": "AA2425",
    "exerciseNo": 5,
    "marks": 2,
    "type": "applied-modeling",
    "topics": ["mongodb"],
    "prompt": "Write a query to compute the total number of (distinct) frames associated with each puzzle based on a picture from \"Leonardo da Vinci\" and whose location is \"Louvre\", returning only the puzzles with more than 10 frames.",
    "solution": "```javascript\ndb.puzzles.aggregate([\n  { $lookup: {\n      from: \"images\",\n      localField: \"imageId\",\n      foreignField: \"_id\",\n      as: \"image\"\n  }},\n  { $unwind: \"$image\" },\n  { $match: {\n      \"image.artistName\": \"Leonardo\",\n      \"image.artistSurname\": \"da Vinci\",\n      \"image.paintingLocation\": \"Louvre\"\n  }},\n  { $project: {\n      _id: 1,\n      name: 1,\n      totalFrames: { $size: { $setUnion: [\"$frameIds\", []] } }\n  }},\n  { $match: { totalFrames: { $gt: 10 } } }\n])\n```\n`$lookup`+`$unwind` join each puzzle to the (single) image it is based on, `$match` restricts to images that are paintings by \"Leonardo da Vinci\" located at the \"Louvre\", `$setUnion` with an empty array de-duplicates `frameIds` before `$size` counts the distinct associated frames, and the final `$match` keeps only puzzles with more than 10 such frames.",
    "solutionSource": "authored"
  }
]
